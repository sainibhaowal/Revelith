/**
 * System font location moved to @revelith/font-metrics (shared with the docs
 * metrics pipeline); re-exported here to keep pdf-main import paths stable.
 */
export { findSystemFont, isTruetype } from '@revelith/font-metrics'
